# 🚀 Setup Instructions

Thank you for downloading **Multi-Role Chatbot (v1.0)**  
Developed by **Madiha Atif**

---

### 1️⃣ Install Dependencies

Make sure you have **Python 3.9+** installed.  
Then open your terminal (or command prompt) and run:

```bash
pip install -r requirements.txt
```

This will install all required libraries (like gradio, openai, etc.).

### 2️⃣ Add Your API Key

Before running the chatbot, set your API key as an environment variable.

On **Windows (PowerShell)**:
setx API_KEY "your_openrouter_api_key_here"

On **macOS / Linux**:
export API_KEY="your_openrouter_api_key_here"


💡 Replace "your_openrouter_api_key_here" with your actual API key.
### 3️⃣ Run the App

After setup, run this command:

python app.py


You’ll see a local Gradio link in the terminal.
Click it to open your chatbot in the browser 🎭

💻 *Using Google Colab? (Optional)*

If you prefer to run it online, upload all files to Colab and execute:

!pip install -r requirements.txt
!python app.py


The chatbot interface will appear with a public link.

### 🧾 Files Included
| File                     | Description                     |
|--------------------------|---------------------------------|
| `app.py`                 | Main chatbot code               |
| `requirements.txt`       | Required dependencies           |
| `setup_instructions.md`  | This setup guide                |
| `assets/`                | Optional images and demo files |

### 📧 Need Help?

If you face any issue or want a custom version (e.g., business bot, translator, or resume assistant), feel free to contact:

## 👩‍💻 Developer: Madiha Atif
## 💼 Fiverr: ahidam_315

